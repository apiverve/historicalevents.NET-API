<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Title>APIVerve.API.HistoricalEvents</Title>
    <Version>1.0.0</Version>
    <Authors>APIVerve</Authors>
    <Company>APIVerve</Company>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>False</PackageRequireLicenseAcceptance>
    <PackageReadmeFile>Readme.md</PackageReadmeFile>
    <PackageProjectUrl>https://apiverve.com/marketplace/api/historicalevents</PackageProjectUrl>
    <SignAssembly>False</SignAssembly>
    <Description>Historical Events is a simple tool for getting historical events. It returns a list of historical events.</Description>
    <RepositoryUrl>https://github.com/apiverve/historicalevents-NET</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>historical events; history; historical events api; history api; historical events data; history data; historical events tool; history tool; historical events software; history software; historical events service; history service; historical events library; history library; historical events sdk; history sdk; historical events code; history code; historical events script; history script; historical events program; history program; historical events application; history application; historical events app; history app; historical events utility; history utility; historical events helper; history helper; historical events helper tool; history helper tool; historical events helper software; history helper software; historical events helper service; history helper service; historical events helper library; history helper library; historical events helper sdk; history helper sdk; historical events helper code; history helper code; historical events helper script; history helper script; historical events helper program; history helper program; historical events helper application; history helper application; historical events helper app; history helper app; historical events helper tool; history helper tool; historical events helper utility; history helper utility; historical events helper</PackageTags>
    <Copyright>©2024 APIVerve. All Rights Reserved</Copyright>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
  </ItemGroup>

  <ItemGroup>
    <None Update="LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="Readme.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

</Project>
